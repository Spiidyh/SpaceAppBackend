version: 0.2

phases:
  install:
    runtime-versions:
      java: corretto11
    build:
      commands:
        - mvn test
    post_build:
      commands:
        - mvn package
artifacts:
  files:
    - target/*.jar
  discard-paths: yes